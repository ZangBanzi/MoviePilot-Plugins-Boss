# 媒体虚拟库交接 · 4.5.0

作者Boss，2026-09-13。项目没有Git元数据。代码、市场索引和前端均为4.5.0，保留4.4.1旧包和4.3.11迁移基线，不得用旧实体归档覆盖当前虚拟库。

用户要求整台Emby服务器批量生成、每库读取自身影片、真正轮播不同海报、原生和虚拟库都能取图，保留原功能和8098/302。参考justzerock呀哈哈封面工坊，本项目独立实现。

## 已完成

- 服务器主选择框和每库编辑卡片。“预览整台服务器”只读；“生成并应用整台服务器”逐库备份后发布原生Primary，并生成当前网关虚拟库历史。后台返回逐库素材数、格式及结果。
- 服务器上下文和原生库ID精确定位；第二服务器的封面操作不改变播放网关。原生按ParentId取材，虚拟仅取本库成员。
- 最多6张不同海报轮播，每张停留1680ms+渐变320ms，包含末到首；不画进度条。0/1张返回静态PNG，预览可暂停，继续支持静态客户端格式。
- 成功的认证首页提供用户ID，随后以 `/Users/{Id}` 验证；尚未取得可信ID时才尝试旧接口，失败仅返回品牌图。
- 无登录头的图片元素通过每用户/每库短期ImageTag凭证取图，逐次重新核验用户与可见成员。凭据仅存在内存，一小时到期、重载失效；不公开管理端历史图片或把API Token写入图片URL。
- 代理压缩图片可解码，鉴权/坏图有静态回退和不可缓存响应；新标签版本使首页重新取图。
- 原生列表刷新成功后原子替换快照，并发预览不读空列表；失败使该服务器旧目标失效。发布仍重新校验并备份。
- 同步任务独立取消标记，停止/重载后旧任务不能继续提交结果，正常取消按INFO记录。

## 现场与权限

用户仅授权NAS只读检查。已确认旧插件4.4.1、Emby4.9.5.0、Users/Me 500和真实用户ID接口200。少量图片及Edge解码成功，所有历史破图原因不能全部断言。

没有远程同步、生成、发布、重启、配置保存、播放或GitHub推送。账号/密码/Token不写入项目、发布包或记忆。本地记忆没有查到相关已存连接记录，使用本次用户提供的信息。不要由“有账号”推断部署许可。

## 不变边界

客户端→NextEmby8098→MoviePilot API3334→原Emby8096，MP前端3333。无新插件端口，无媒体文件操作，无ItemId/MediaSource/Location改写，无Collection/BoxSet创建。原生发布是显式操作，原Cron/增量不发布原生图。

字体、配置、历史、缓存与权限继续保留。发布必须完整包含 `plugins.v2/mediaarchiver/` 下Python、fonts、dist/assets及requirements；前端源码在frontend，NAS不需要Node。

## 验证与交付

68项pytest、4个独立脚本、17个浏览器场景通过，最终无失败工坊API或页面异常。测试覆盖冷封面/原302联合运行、同名库/第二服务器、权限撤销/到期、gzip图片、原生备份恢复。详见TEST_REPORT。

最新包 `releases/MediaArchiver-v4.5.0.zip`，逐文件校验SHA256SUMS，未自动上传或部署。完整上传后择时重启MP，使主/子模块及旧任务统一换新；health的version与cover_engine应均为4.5.0。现场按ACCEPTANCE验收。

豆瓣tv_global、猫眼证据不足、IMDb/Bangumi访问仍是独立问题，不混用榜单掩盖。腾讯确认空榜和混合榜部分更新已有回归；日志中部分旧措辞不在已核对的4.4.1源码中，不能仅凭健康接口排除旧线程。

旧记录见 `docs/HANDOFF-4.4.1.md`、`docs/TEST_REPORT-4.4.1.md` 和更早历史。引用的ChatGPT任务只取得用户提供的缓存预览，没有读取完整逐字历史。
