# MediaArchiver 4.3.10 验证记录

本次在此前交付的 4.3.10 上追加伦理误收修复，继续使用版本号 4.3.10。附件中的 1.0.0 实体归档代码未作为本次基线。

## 修改范围

未引入新依赖。伦理规则由全文关键词改为完整标签/题材类型匹配；取消简介、标题、路径、厂牌和年龄分级触发，增加 Emby 标签人工加入/排除。原有 PNG 封面、页面、配置、Cron、3334 网关、192 组访问缓存和 8098 访问链路保留。其他平台识别、ID 匹配和榜单来源策略尚未按后续建议重写。

截图显示的是“文件夹”页，因此同时验证根级、用户级、带 /emby 前缀的分页入口只返回指定虚拟库成员。未取得真实客户端请求和影片元数据，不能据此断言截图每一条目的实际误判原因，或所有客户端分页行为均已验证。

## 回归结果

- 原有独立功能回归通过：属性和榜单匹配、IMDb/TMDB热门兜底、AniList/TMDB动漫兜底、TVB 剧集专区、伦理专区、多版本、去重、增量校准、Cron、动态封面、首页 Views、Items/Latest、分页、原 ItemId、302 和标准库回退。
- 新增 **18 组伦理规则样本**：先在旧代码上复现“长大成人”误收，再验证修复。覆盖家庭伦理、Adult Swim、年龄分级、父目录、畸形标签、明确题材、人工加入/排除、电影/剧集和情色动画。片名取自截图的条目使用构造字段，并非用户实际 Emby 元数据。
- 新增同步检查：将全部样本模拟为旧专区成员，验证重建清除误收、重复同步稳定、标签变更增删成员且原条目不被修改。
- 新增 **15 组分页请求检查**：三种 Items 入口分别检查两页、越界空页及电影/剧集过滤。上游故意返回额外条目，验证专区外 ItemId 不会被透传进入结果。此项验证了现有筛选路径，未据截图猜测或改写未知客户端请求。
- 真实 HTTPX/FastAPI 协议测试 **24 项全部通过**（本次执行约 2.03 秒）。首次收集测试时，本地测试环境的 pydantic_core 二进制大小和 RECORD 校验不符导致 Bus error；从同版本运行环境恢复并核对 RECORD 后通过测试。未更改插件依赖。
- 既有压缩测试覆盖 gzip、Brotli、deflate、raw-deflate、Zstd、叠加压缩、缺失/错误压缩头、UTF-16 JSON、大小限制和只读重试。
- 既有转发测试覆盖 Cookie 隔离、多个 Set-Cookie、编码路径、中文请求头、HEAD/204/304/401、Range/302、流式首块、断开回收、32 并发连接与 WebSocket。
- 新检查验证 IMDb 页面无条目时返回 TMDB 热门兜底，AniList 403 时返回 TMDB 动漫兜底，混合榜在部分子榜 404 时仍返回有效条目。4.3.9 检查继续覆盖电影和剧集都可进入 `伦理专区`，且 `AV1` 视频编码不会被误判为成人内容。

## 历史本地性能对比（4.3.6 / 4.3.7，本次未重测）

固定输入：7000 个唯一媒体条目，额外加入 500 条重复记录。上游返回内存中的固定列表，排除网络、磁盘和外部榜单访问；混合电影与剧集，包含多种 Provider ID、重复标题和媒体源字段。

在两版之间交替执行，每个场景测量 5 次，计时前进行垃圾回收，下表时间为中位数。内存使用另一次独立的 tracemalloc 测量，表示输入已建立后，该操作新增 Python 分配的峰值，**不是整个 MoviePilot 的 RSS**。

| 场景 | 4.3.6 时间 | 4.3.7 时间 | 4.3.6 峰值分配 | 4.3.7 峰值分配 |
|---|---:|---:|---:|---:|
| 单独构建榜单索引 | 0.1350 s | 0.0855 s | 12.06 MiB | 12.00 MiB |
| 只同步属性专区 | 0.3625 s | 0.2165 s | 14.18 MiB | 2.07 MiB |
| 同时同步属性与榜单 | 0.2910 s | 0.2533 s | 14.08 MiB | 13.97 MiB |

纯属性场景不再建立无用榜单索引，峰值分配约减少 85%；启用榜单后仍需建立索引，因此内存下降较小。两种同步模式的扫描统计、虚拟库成员、类型、封面标签和常驻索引均与 4.3.6 相同（比较时排除每次变化的同步时间）。

这些是本地固定数据结果，不代表 NAS 实际响应速度，也不构成可承载用户人数承诺。

## 环境与发布核验

HTTP 出站使用本地回环 HTTP 服务；MoviePilot 专用对象以替身提供；未连接用户 NAS。本次伦理策略不添加在线查询或读取影片字节，识别仍在后台同步阶段完成。

插件和索引版本统一为 **4.3.10**，作者 Boss。升级后健康接口应返回同一版本与下列源码指纹；客户端仍通过 NextEmby 8098 访问，原播放链路与媒体文件不变。

源码 SHA-256：`4e2128249c50c14962bdf1c4a164bdef1758932a488dbd6809a586a00a51b32e`

复现回归的命令见 README。发布包包含测试代码和 SHA256SUMS；部署后的 Emby/NextEmby/客户端实际行为仍需现场验证。
