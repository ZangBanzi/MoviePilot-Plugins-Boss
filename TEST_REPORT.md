# MediaArchiver 4.3.7 验证记录

本次使用 ponytail 做最小重构，基于已交付的 4.3.6 源码（SHA-256：`3e48e01880e56cceba3dfb14e881c2b70ef544deb981ce5d388176da912db0f5`）。附件中的 1.0.0 实体归档代码未作为本次基线。

## 修改范围

插件源文件增加 36 行、删除 60 行，净减少 24 行；未引入新依赖。主要改动为统一去重、按需构建榜单索引、减少列表复制和 Provider 别名重复解析、在锁外生成媒体快照、后台生成与复用封面。删除两个无调用方法；原有 PNG 封面、页面、配置及功能入口保留。

冷封面使用独立锁串行绘制，避免同一封面并发重复生成。此选择优先限制 CPU 使用，并未实现多封面并行绘图。

## 回归结果

- 原有独立功能回归通过：属性和榜单匹配、多版本、去重、增量校准、Cron、动态封面、首页 Views、Items/Latest、分页、原 ItemId、302 和标准库回退。
- 真实 HTTPX/FastAPI 协议测试 **24 项全部通过**（本次执行约 2.29 秒）。其中 23 项沿用 4.3.6，1 项综合检查本次重构。
- 既有压缩测试覆盖 gzip、Brotli、deflate、raw-deflate、Zstd、叠加压缩、缺失/错误压缩头、UTF-16 JSON、大小限制和只读重试。
- 既有转发测试覆盖 Cookie 隔离、多个 Set-Cookie、编码路径、中文请求头、HEAD/204/304/401、Range/302、流式首块、断开回收、32 并发连接与 WebSocket。
- 新检查验证 Provider 别名、重复标题和同名不同年份匹配不变；同步构造新快照时旧索引仍可读取，发布后整体切换；并发请求同一冷封面只绘图一次，绘图等待期间健康接口仍能响应。

## 本地性能对比

固定输入：7000 个唯一媒体条目，额外加入 500 条重复记录。上游返回内存中的固定列表，排除网络、磁盘和外部榜单访问；混合电影与剧集，包含多种 Provider ID、重复标题和媒体源字段。

在两版之间交替执行，每个场景测量 5 次，计时前进行垃圾回收，下表时间为中位数。内存使用另一次独立的 tracemalloc 测量，表示输入已建立后，该操作新增 Python 分配的峰值，**不是整个 MoviePilot 的 RSS**。

| 场景 | 4.3.6 时间 | 4.3.7 时间 | 4.3.6 峰值分配 | 4.3.7 峰值分配 |
|---|---:|---:|---:|---:|
| 单独构建榜单索引 | 0.1350 s | 0.0855 s | 12.06 MiB | 12.00 MiB |
| 只同步属性专区 | 0.3625 s | 0.2165 s | 14.18 MiB | 2.07 MiB |
| 同时同步属性与榜单 | 0.2910 s | 0.2533 s | 14.08 MiB | 13.97 MiB |

纯属性场景不再建立无用榜单索引，峰值分配约减少 85%；启用榜单后仍需建立索引，因此内存下降较小。两种同步模式的扫描统计、虚拟库成员、类型、封面标签和常驻索引均与 4.3.6 相同（比较时排除每次变化的同步时间）。

这些是本地固定数据结果，不代表 NAS 实际响应速度，也不构成可承载用户人数承诺。

## 环境与发布核验

Python 3.12、HTTPX 0.28.1、FastAPI 0.115.14、Starlette 0.46.2、APScheduler 3.11.0、Brotli 1.1.0、Zstandard 0.25.0、websockets 16.0、pytest 8.4.1。HTTP 出站使用真实回环 HTTP 服务；MoviePilot 专用对象以替身提供；未连接用户 NAS。

插件和索引版本统一为 **4.3.7**，作者 Boss。升级后健康接口应返回同一版本与下列源码指纹；客户端仍通过 NextEmby 8098 访问，原播放链路与媒体文件不变。

源码 SHA-256：`44988e4ab3e1e228f4d1db8c138d7b7066534f51547612504481c368197522ad`

复现回归的命令见 README。发布包包含测试代码和 SHA256SUMS；部署后的 Emby/NextEmby/客户端实际行为仍需现场验证。
