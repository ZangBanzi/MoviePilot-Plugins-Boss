# MediaArchiver 4.3.10 验证记录

本次使用此前交付的虚拟库 4.3.10 为基线，附件中的旧 1.0.0 实体归档代码不作为基线。版本仍为 4.3.10，作者 Boss。

## 修改范围

逐媒体源判断属性，结构化参数优先；TVB/伦理采用完整标签取证；榜单匹配约束类型、ID、年份和标题唯一性；取消跨来源榜单兜底；更新地区候选合并、混合榜失败与旧快照迁移策略。删除未使用的全文字段展开方法和导入。

原有 3334 网关、8098 访问链路、原 ItemId、MediaSource、302、封面、Cron、浏览缓存保留。没有增加依赖或读取媒体文件内容。

## 已执行验证

- tests/test_accuracy.py：通过。包括 21 组属性样本，以及身份冲突、同名消歧、IMDb 榜单主体、AniList 失败、地区合并排序、可信快照保留与旧快照失效检查。
- tests/test_virtual_library.py：通过。包括 18 组伦理样本、重建清理/标签变更检查和 15 组分页请求检查；同时覆盖既有同步、Cron、封面、去重、原 ItemId 和 302 行为。已将旧跨来源兜底预期改为失败保留可信快照。
- tests/test_gateway_runtime.py：24 项全部通过（2.10 秒）。覆盖真实 HTTPX/FastAPI 与本地上游的压缩、Cookie 隔离、流式响应、断开回收、Range/302、32 并发、WebSocket，以及同步/封面不阻塞浏览。
- 测试环境曾发生 pydantic_core 二进制与 RECORD 不符的 Bus error；恢复同版本、校验一致的二进制后通过，未修改插件依赖。

## 边界

样本使用构造的 Emby 元数据，不是用户 NAS 真实条目。没有现场客户端测试，也没有实测误判率或本次 NAS 吞吐基准；不能把回归通过理解为全部客户端适配或准确率承诺。

更严格的规则会跳过证据不足条目。技术属性仍为电影范围；TVB、伦理和对应平台选项支持剧集。外部源实时页面可变，结构化解析仍可能遇到源站变更；失败不伪造其他来源结果。

源码 SHA-256：`64decb134210cb8a3f94afb9fe48cfddf436a5fabc0682214da00667228a00eb`

升级同版本后重启 MoviePilot，对照健康接口 code_sha256，再执行一次一键重建。SHA256SUMS 覆盖发布文件；复现命令见 README。
