# 4.5.1 用户环境验收

本地87项后端测试、4个独立脚本、21个浏览器场景通过。本次未连接或改动NAS，新版尚未部署。

## 升级

完整上传 `plugins.v2/mediaarchiver/` 和根目录 `package.v2.json` 的MediaArchiver条目，保留其他插件。coverstudio.py、dist/assets、fonts、requirements.txt不能遗漏。

刷新市场升级4.5.1，在合适时间重启MoviePilot以统一加载子模块并结束旧任务；此次没有代为重启。保持8098→3334→8096。

读取 `http://NAS地址:8098/__mediaarchiver__/health`（或直连3334），确认version和cover_engine均为4.5.1，code_sha256匹配SHA256SUMS中的主文件。reload-required表示仍加载旧封面子模块。

## 封面

1. 打开工坊，主下拉框为“Emby 服务器”。原生/虚拟库各有独立卡片，同名库显示不同ID。
2. 点击“预览整台服务器”，检查原生库和含同一批影片的虚拟专区都能取图，且素材来自各自成员。此步骤不覆盖Emby图片。
3. 点选库卡片，使用Backdrop或Primary，核对标题、素材数和布局。至少两幅不同素材时播放动图预览，检查海报停留、切换、循环；暂停回到静态。0/1张素材静态输出是预期行为，画布及生成后的历史会明确说明空库/单图；有成员但取图失败另行提示。
4. 点击“生成并应用整台服务器”，检查 GIF、静态和失败数，并在“运行状态”查看每库具体原因。原生库更新前有自动备份；无图、备份失败、目标失效时保留原图并报告失败。虚拟库历史生成后，客户端仍按自身权限取图。
5. 刷新8098客户端首页，让新ImageTag生效。图片应可解码，素材不可用时显示静态品牌图。响应头X-MediaArchiver-Cover可查看artwork/fallback/no-artwork状态。
6. 历史中“更新前备份”的“恢复原生库图片”恢复实际旧图；“恢复此方案”只恢复设计参数。

## 本次问题复核

- 在此前报 3 MiB 错误的库先预览，再更新该库封面。旧图不超过64 MiB且格式可解码时应成功备份；从历史下载原图核对其格式，GIF仍保留动画。
- 伦理/猫眼等仍为0部时应显示“空库”原因和静态品牌封面；待本库有至少两幅不同海报，再预览GIF。不要通过其他库素材伪装成功。
- 超限或备份失败时应明确标明“备份原生库旧封面”，保留当前原图，不应仅显示笼统失败。
- 旧历史没有原因字段，需要重新生成才能得到准确诊断；旧文件格式标签应与下载文件一致。

## 原功能

- 原生/虚拟库进入同一影片，检查原ItemId、MediaSource和302直链，入口仍为8098。
- 后台生成时浏览/播放其他影片，检查分页、Latest及既有专区。
- 受限用户的封面不出现无权访问的影片；撤销原用户权限后旧ImageTag不能返回旧海报，失效请求静态回退。
- 原生图不由原Cron/增量自动发布。停止/重载产生的取消保留已有虚拟库，不应误报为源站同步失败。

遇到问题保留版本/主文件指纹、库名与ID、图像格式、响应状态、插件提示和相邻日志，去掉Key/Token/Cookie。外部榜单失效与封面/播放分别核验，不因源站失败放宽身份识别。
