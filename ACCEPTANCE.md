# 4.4.1 用户环境验收

本地59项后端测试、4个独立回归脚本、13个浏览器场景已通过。下面用于安装后的NAS现场验收；本次没有连接你的NAS或代为上传。

## 1. 确认加载的是新包

完整上传 plugins.v2/mediaarchiver/（含 coverstudio.py、dist/assets/、fonts/、requirements.txt）和根目录 package.v2.json。刷新插件市场升级4.4.1，重启MoviePilot。

读取 `http://NAS地址:3334/__mediaarchiver__/health`，确认 version=4.4.1、code_sha256 与 SHA256SUMS 主文件一致。保存原有配置并一键重建，让旧索引补齐海报标签。原先已接通的端口配置保持8098→3334→8096。

## 2. 同一组影片，两类库分别验证

选择一个有电影/剧集海报的原生库，以及包含其中影片的虚拟专区（例如4K/Remux）。

1. 工坊点击“读取原生媒体库”，选择原生库，海报来源选 Backdrop 或 Primary。确认显示“已读取…幅 Emby 海报”、正确库名/数量；分别预览叠影和映墙。
2. 切换对应虚拟库，用相同海报来源和布局，确认也能匹配海报。按预设/随机顺序选出的影片可能不同，要求是各自权限内能找到素材并生成，画面无需完全一致。
3. 虚拟库点击“应用封面方案”，在8098客户端刷新图片。分别验证静态模式和GIF模式；播放器只显示GIF首帧属于客户端能力，先用浏览器图片地址判断实际格式。
4. 回到原生库。“生成当前封面”只保存历史；“生成并更新原生库封面”才更新Emby库图。核对只改所选库图片，历史中存在“更新前备份”，再试“恢复原生库图片”。
5. 同名原生库按选择项对应的不同ID处理，不能互相覆盖。原生库没选中时更新按钮不出现。

## 3. 原功能与播放验收

- 分别从原生库、虚拟库进入同一部影片并播放，确认客户端仍连8098，使用原ItemId和MediaSource，原302直链正常。
- 查看完整列表、下一页、最新加入、原有属性/榜单专区；试一次增量校准和既有Cron配置。
- 用受限Emby用户验证虚拟库海报不出现其无权观看的影片素材；更换/撤销凭据后旧图片缓存不能绕过权限。
- 后台生成封面时打开其他库/播放原影片，确认交互正常；生成任务可停止。

## 遇到问题时保留的信息

记录健康接口版本/指纹、出问题的是原生还是虚拟库、所选布局与素材来源、插件页面提示和相邻日志。日志中移除Key/Token/Cookie后再提供。原生取图失败时先不要改选别的同名库；系统会保留旧图并显示错误。

本版没有修改此前豆瓣tv_global、IMDb访问、AniList/Bangumi超时等源站限制，这些与封面/302验收分开判断。
