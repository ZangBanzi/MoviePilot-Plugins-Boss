# 媒体虚拟库交接 · 4.5.2

作者 Boss，2026-09-13。项目没有 Git 元数据。当前代码、市场索引和前端为4.5.2。保留4.5.1旧包及历史文档，旧实体归档不得覆盖当前虚拟库。

用户本次要求：选择光幕后批量生成仍跳回叠影；加入媒体库全选/反选/单独勾选；历史卡片删除与一键清空全部。

## 实现

- 原因是旧前端只保存当前库，批量任务逐库读旧方案。新接口接收明确keys与可选options，校验全部目标归属后一次保存选中库方案，并为本次任务固定逐库参数；虚拟库播放标签同步更新。
- 默认“统一使用当前方案”：勾选库都使用当前完整参数，包括字体/标题；主标题留空跟随各库名称。未勾选库不改。“使用各库已保存方案”严格只读已保存参数，不自动应用任何草稿。
- 复选框决定批量范围，与卡片编辑按钮分开；支持全选和反选，0项禁用批量操作，刷新保留选择，切换服务器重建选择范围。每库未保存的编辑在当前页面保留，切回恢复；刷新整个页面不会保存草稿到服务器。
- 单库生成直接提交当前options并写入该库覆盖，不会因为界面“设为默认方案”而继续读取旧独立方案。批量预览按同一模式取材，但不保存或上传图片。
- 历史卡片删除、全量清理覆盖所有批次/服务器，包括页面60条之外的记录和原生旧图备份。清空需confirm=true；清理和生成/恢复共享job_lock。路径仅限history下校验过的UUID图片，非递归，不删除其他数据或Emby图片。
- 先提交索引再删文件，失败保留可重试记录；索引恢复失败的残留可再次全量清理。索引已空时仍有“清理历史残留”入口，结果明确显示失败/残留清理数量。

## 不变边界

保留8098→MP API3334→Emby8096，前端3333。无新增插件端口，无原媒体文件操作，无ItemId/MediaSource/302 Location改写，无Collection/BoxSet。

原生图片在用户明确生成并应用时才备份后上传，Cron/增量不发布。当前用户权限取材、短期ImageTag、静态回退、真实多海报GIF、大图原件64MiB/素材16MiB/元数据16MiB限额与256MiB历史总量均保留。空库和单图仍按实际素材静态输出。

历史清空包括原生备份，界面有明确确认说明；已应用图片、方案、字体、配置备份保留。此操作不是清除内存图片缓存。

## 验证与交付

117项pytest与4个独立脚本、31个浏览器场景通过（26真实API流程，5固定响应展示）；证据见TEST_REPORT与docs/BUILDINFO.json。新增测试为test_batch_selection.py、test_history_cleanup.py、ui_selection.mjs。

主文件141个方法中138个AST与4.5.1相同，另外3个只更新User-Agent版本字符串；播放、权限、识别、同步未改动。主要变更在coverstudio及Vue工坊。

发布包releases/MediaArchiver-v4.5.2.zip须完整上传插件目录（Python、fonts、requirements、dist/assets）及根索引MediaArchiver条目。SHA256SUMS覆盖包内文件；NAS不需要Node。health的version与cover_engine应均为4.5.2。

本次仅本地修改、回环测试和打包，未连接NAS、未远程生成/清理/重启/部署或推送GitHub。此前的NAS只读授权不能视作部署授权，凭据不写入项目/发布包/记忆。旧报告见docs/HANDOFF-4.5.1.md、docs/TEST_REPORT-4.5.1.md。
